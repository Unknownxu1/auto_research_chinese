# autoresearch

这是一个用于普通机器学习任务的实验仓库。

核心目标很简单：基于给定的数据集和评估指标，迭代 `train.py`，尽量把验证效果做好，并把每次尝试记录清楚。

## 基本原则

1. 只把 `train.py` 当作主要可编辑入口。
2. `prepare.py` 通常视为固定的数据准备和评估辅助脚本，不建议改动。
3. 每次实验都应该是一次完整、可复现的训练尝试。
4. 优先选择能稳定带来提升的简单方法，而不是堆砌复杂技巧。
5. 如果某个想法会让代码明显复杂，但收益不确定，就先不要用。

## 你要做什么

当开始一个新的机器学习实验时，建议按下面的顺序推进：

1. 查看 `README.md`，理解任务背景、数据格式和目标指标。
2. 查看 `prepare.py`，确认数据读取、特征处理、评估方式和固定约束。
3. 只修改 `train.py`，实现模型、特征工程、训练流程和推理逻辑。
4. 运行训练并观察验证指标。
5. 将本次实验结果写入 `results.tsv`。
6. 根据结果决定下一次尝试是保留、丢弃还是继续优化。

## 训练方式

普通机器学习任务通常包含以下元素：

- 数据清洗
- 特征工程
- 训练/验证切分
- 模型选择
- 超参数调优
- 结果记录

你可以在 `train.py` 中自由尝试这些内容，但要尽量保持实验目标清晰。典型做法包括：

- 对表格数据使用线性模型、树模型、集成模型或轻量神经网络。
- 对时序数据使用时间切分、滞后特征、滚动统计特征。
- 对分类任务关注 `accuracy`、`f1`、`auc` 等指标。
- 对回归任务关注 `rmse`、`mae`、`mse` 等指标。

如果仓库已经定义了主评估指标，就以它为准。

## 约束

- 不要修改 `prepare.py`，除非用户明确要求。
- 不要随意增加新的依赖，优先使用 `pyproject.toml` 里已有的库。
- 不要为了很小的提升引入过度复杂的实现。
- 如果实验失败或效果变差，要能清楚说明原因。

## 输出与记录

每次实验结束后，建议记录以下信息：

- 当前提交的 git hash
- 验证集指标
- 训练耗时
- 主要模型配置
- 这次尝试的简短说明

`results.tsv` 可以采用如下格式：

```tsv
commit	metric	score	status	description
```

其中：

- `commit` 是 git 提交哈希的短格式。
- `metric` 是本次使用的评估指标名称。
- `score` 是对应数值。
- `status` 通常填 `keep`、`discard` 或 `crash`。
- `description` 用一句话说明本次实验做了什么。

示例：

```tsv
commit	metric	score	status	description
a1b2c3d	rmse	0.8421	keep	加入日期特征和目标编码
b2c3d4e	rmse	0.8513	discard	增大模型但验证集变差
c3d4e5f	auc	0.0000	crash	特征维度处理出错
```

## 推荐流程

一个比较稳妥的实验循环是：

1. 读取当前代码和数据说明。
2. 选一个简单、可解释的基线模型。
3. 跑通训练和验证。
4. 记录结果。
5. 只做一个方向明确的改动。
6. 再次训练并比较结果。

如果改动有效，就保留；如果无效，就回退并尝试别的方向。

## 给机器学习助手的提示

你可以把这份文件理解成给自动实验助手的工作说明：

- 目标是做好一个普通的机器学习任务，而不是研究 LLM 自主实验。
- 重点是稳定改进验证指标。
- 允许反复尝试，但每次尝试都应该可追踪、可比较。
- 尽量把实验过程保持简洁、明确、可复现。

